operations_list = ['plus', 'minus', 'multiply']
selected_operation = None

for operation in operations_list:
    if operation in ['plus', 'minus', 'multiply']:
        selected_operation = operation
print(selected_operation)
